/** One page gutter, shared by the navbar, the app body and the footer so the
 *  three line up at every breakpoint. Matches the other Universal Apps. */
export const CONTAINER = 'w-full max-w-6xl mx-auto px-4 sm:px-6'
